import React from 'react';
import { shallow, configure } from 'enzyme';
import Adapter from 'enzyme-adapter-react-16';
configure({ adapter: new Adapter() });

import ListTimers from './Timers';
// import 


describe('TimerList', () => {
    it('should pass in prop correctly and render correctly', () => {
        // const timer = new
        // Make an array with x number of timers
        
        // var a = []
        // for ( count to 88) {
        //     a.push({name:"Timer"+count, time:0, isRunning: false})
        // } 
       
        // Make an instance of <Timers /> pass the array as props
        // test...
        // expect a div with class timerContainer
        // expect x number of divs with class eachTimer
    })
})